{
  "name": "mesh-landing",
  "private": true,
  "version": "1.0.0",
  "description": "Mesh — Communication Without Internet. P2P offline messaging landing page.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
